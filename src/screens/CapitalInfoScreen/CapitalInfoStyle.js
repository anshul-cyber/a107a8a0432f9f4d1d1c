const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "beige",
  },

  label: {
    fontWeight: "bold",
    color: "#000",
  },

  iconPlate: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
});
